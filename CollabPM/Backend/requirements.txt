# --- Core web framework ---
Flask==3.1.3
python-dotenv==1.0.1          # loads Backend/.env into environment variables

# --- Database / ORM (used from Step 2 onwards) ---
Flask-SQLAlchemy==3.1.1       # the ORM: Python classes <-> Postgres tables
Flask-Migrate==4.0.7          # Alembic migrations: version-control the schema
psycopg2-binary==2.9.10       # the PostgreSQL driver SQLAlchemy talks through

# --- Auth (used from Step 3 onwards) ---
bcrypt==4.2.1                 # one-way password hashing
Flask-JWT-Extended==4.7.1     # issue & verify JWT access tokens

# --- Frontend integration ---
Flask-Cors==5.0.0             # let the Next.js app (localhost:3000) call the API

google-auth==2.35.0

requests==2.32.3